/**
 * Work-edition stub for the inline panels that show authored material — the consulting lens and the links to
 * sibling applications. Both render nothing here, so the pages that embed them need no edition branching.
 */
export function PepiLens() { return null }
export function HubLinks() { return null }
