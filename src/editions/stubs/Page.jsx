import { PageHeader } from '../../components/primitives/index.js'

/** Work-edition stub page: this module is not part of this build. */
export default function NotInThisEdition() {
  return <PageHeader eyebrow="Not in this edition" tone="muted" title="This module isn't part of the research edition." lede="It contains authored material rather than public records, so it is excluded from this build." />
}
